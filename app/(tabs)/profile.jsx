import { View, Text } from 'react-native'
import React from 'react'
import { StyleSheet, SafeAreaView } from 'react-native';

export default function profile() {
  return (
    <SafeAreaView>
    <View>
      <Text style={{fontSize:40, fontFamily:'outfit-thin'}}>profile</Text>
    </View>
    </SafeAreaView>
  )
}